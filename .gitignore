# Credentials / local runtime configuration
.env
.env.*
!.env.example

# Python / test caches
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
.venv/
venv/

# Runtime evidence and local browser state
.backend_runtime/
.hip_runtime/
runs/
uploads/*
!uploads/.gitkeep
# What the agent learns about each form on a user's machine (V243R17)
data/hip_memory/form_structure_memory/
# Which recovery step (refresh / browser restart) fixed each phase (V243R18)
data/hip_memory/runtime_recovery_ladder.json

# Build output
build/
dist/

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Built wheels (rebuilt from source; shipped in release zips, not git)
*.whl
release/
