{
  "name": "hip-portal-agent-javascript-ui",
  "version": "1.9.2",
  "private": true,
  "description": "Bun-served JavaScript control center plus MCP tooling for the governed HIP Portal agent",
  "devDependencies": {
    "@playwright/mcp": "0.0.79",
    "chrome-devtools-mcp": "1.6.0"
  },
  "scripts": {
    "api": "python -m uvicorn backend.app:app --host 127.0.0.1 --port 8000",
    "ui": "bun webui/server.js",
    "platform": "bun webui/platform.js",
    "start": "bun webui/platform.js",
    "ui:legacy-streamlit": "python -m streamlit run streamlit_app.py --server.port 8503 --server.address localhost --server.fileWatcherType poll",
    "test:python": "python -m pytest tests -q",
    "test:js": "node --check webui/app.js && node --check webui/server.js && node --check webui/platform.js",
    "mcp:playwright": "npx @playwright/mcp@0.0.79",
    "mcp:chrome": "npx chrome-devtools-mcp@1.6.0 --no-usage-statistics",
    "mcp:playwright:bun": "bunx --bun @playwright/mcp@0.0.79",
    "mcp:chrome:bun": "bunx --bun chrome-devtools-mcp@1.6.0 --no-usage-statistics"
  },
  "engines": {
    "bun": ">=1.2.0"
  },
  "packageManager": "bun@1.3.13",
  "type": "module"
}
