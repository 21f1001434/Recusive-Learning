# Acceptance Test Results

```text
123 passed
```

Scope verified:

- Existing Document Type KB tests remain green.
- Deep profile parser extracts `/api/document-type/{id}/details` payload fields.
- Deep profile fields are merged back into each inventory record.
- CLI exposes full deep-profile capture options.
